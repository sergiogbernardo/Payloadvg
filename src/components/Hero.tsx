export default function Hero() {
  return (
    <section className="mb-8 text-center">
      <h1 className="font-display text-3xl font-bold tracking-tight text-slate-100 sm:text-4xl">
        Biblioteca de <span className="text-emerald-400">payloads</span>
      </h1>
      <p className="mx-auto mt-3 max-w-2xl text-sm text-slate-400 sm:text-base">
        Payloads, reverse shells parametrizáveis e encoders, prontos pra copiar — no navegador.
      </p>
    </section>
  );
}
